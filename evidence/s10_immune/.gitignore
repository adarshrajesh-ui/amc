# Retrieved publisher full texts and figure images: a local retrieval cache used during
# extraction, not a deliverable. Every number extracted from these files is quoted verbatim
# in the YAML records, and _tools/getft.sh + _tools/html2txt.py regenerate the cache.
_raw/
