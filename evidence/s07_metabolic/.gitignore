# Retrieved publisher full texts (JATS XML / PDF): a local retrieval cache used during
# extraction, not a deliverable. Every number extracted from these files is quoted verbatim in
# the YAML records, and _tools/pmfetch.sh + _tools/x2t.py regenerate the cache from the DOIs
# and PMCIDs recorded in each record's `url` field.
_ft/
